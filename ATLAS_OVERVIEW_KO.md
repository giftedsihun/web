# Atlas Browser: 구현 현황과 계획

## 1. 프로젝트 목적

Atlas Browser는 웹을 단순히 열람하는 도구가 아니라, 사용자가 방문하고 수집한 문서를 개인 지식 기반으로 축적하고 다시 검색할 수 있게 만드는 데스크톱 브라우저입니다.

핵심 방향은 다음과 같습니다.

- Chromium 기반으로 일반 웹을 탐색한다.
- 문서의 제목, 본문, 구조, 링크를 로컬에 색인한다.
- 링크와 노트를 연결해 탐색 경로를 지식 그래프로 보여 준다.
- 검색 연산과 메모를 통해 개인이 수집한 웹을 재발견하게 한다.

현재 단계는 **개인용 로컬 지식 브라우저 및 검색 엔진 MVP**입니다. 전 세계 웹을 대상으로 하는 중앙집중형 검색 서비스는 아직 범위에 포함하지 않습니다.

## 2. 현재 아키텍처

```text
Electron main process
  |- BrowserWindow + Chromium webview
  |- IPC API
  |- bounded web crawler
  `- SQLite database (documents, links, notes, FTS5)

Renderer process
  |- tabs and browser controls
  |- page structure and knowledge graph
  |- local search and crawler controls
  `- knowledge-note interface

Local storage (Electron user-data)
  |- atlas-search.sqlite
  |- atlas-tabs.json
  `- knowledge-browser.json
```

### 기술 구성

- Electron: 데스크톱 창, Chromium `webview`, IPC
- TypeScript: 메인/프리로드/렌더러 코드
- Node.js `node:sqlite`와 SQLite FTS5: 문서 및 노트 전문 검색
- HTML/CSS/SVG: 브라우저 UI와 링크 그래프

## 3. 구현된 기능

### 브라우징

- 탭 생성, 닫기, 활성화
- 주소 입력과 검색어 입력 시 Google 검색 이동
- 뒤로, 앞으로, 새로고침, 북마크
- `Ctrl+L` 주소창, `Ctrl+T` 새 탭
- 열린 탭, 활성 탭, 최근 닫은 탭을 로컬에 저장
- 앱 재시작 시 탭 세션 복원
- `Ctrl+Shift+T` 또는 탭 바의 복원 버튼으로 최근 닫은 탭 열기

### 문서 분석과 색인

- 방문한 페이지에서 제목, 본문, `h1`~`h3`, 링크를 추출
- 문서당 본문은 최대 12,000자, 헤딩은 최대 35개, 링크는 최대 40개 저장
- URL을 기준으로 기존 색인을 갱신해 중복 문서를 줄임
- 링크 관계를 SQLite `document_links`에 보관
- 이전 JSON 기반 문서가 있으면 첫 SQLite 실행 시 자동 이전

### 로컬 검색 엔진

- SQLite FTS5로 문서와 지식 노트를 함께 검색
- `AND`, `OR`, `NOT`, 괄호, 큰따옴표 구문 검색 지원
- SQLite `bm25` 순위로 문서 결과를 정렬
- 문서와 노트는 하나의 결과 화면에서 함께 반환
- 잘못된 검색식은 입력 형식 안내를 표시

### 제한형 웹 크롤러

- 사용자가 지정한 시작 URL에서 연결된 HTML 페이지를 수집
- 기본값 10페이지, 한 번에 최대 50페이지로 제한
- 시작 도메인과 동일한 호스트만 수집
- `robots.txt`의 `User-agent: *`와 `Disallow`를 확인
- 요청 사이에 350ms 간격을 두며, HTTP/HTTPS 및 HTML 응답만 수집
- 외부 도메인, 비 HTML 응답, 실패 페이지와 리다이렉트된 외부 도메인은 건너뜀
- 수집 결과는 기존 색인, 검색, 링크 그래프에 즉시 반영

### 지식 그래프와 노트

- 저장된 문서와 링크를 SVG 그래프로 시각화
- 원형은 문서, 사각형은 해당 원문에 연결된 노트
- 그래프의 문서 노드를 눌러 해당 URL로 이동
- 웹페이지에서 문장을 선택해 인용문, 메모, 태그를 저장
- 노트 수정, 삭제, 태그별 필터링 지원
- 노트는 원문 URL과 제목을 유지하며 검색 및 그래프에 연결

### 로컬 데이터

| 데이터 | 저장 위치 | 내용 |
| --- | --- | --- |
| 검색 색인 | `atlas-search.sqlite` | 문서, 링크, 노트, 태그, FTS5 색인 |
| 브라우저 상태 | `knowledge-browser.json` | 북마크, 방문 기록, 이전 버전 문서 |
| 탭 세션 | `atlas-tabs.json` | 열린 탭, 활성 탭, 최근 닫은 탭 |

이 파일들은 프로젝트 폴더가 아니라 운영체제의 Electron 사용자 데이터 디렉터리에 생성됩니다. 현재 데이터는 기본적으로 사용자의 컴퓨터 안에만 남습니다.

## 4. 사용 방법

```powershell
npm install
npm start
```

1. URL을 열거나 주소창에 검색어를 입력합니다.
2. 왼쪽의 `로컬 검색`을 열어 방문한 문서를 검색합니다.
3. 웹 수집 영역에 사이트의 시작 URL과 수집할 페이지 수를 입력합니다.
4. 수집 완료 후 같은 검색창에서 새 문서를 검색하거나 `지식 그래프`에서 링크 관계를 확인합니다.
5. 웹 문장을 선택하고 `NOTE +`를 눌러 개인 노트를 연결합니다.

## 5. 현재 범위와 한계

현재 Atlas는 안전하게 범위를 제한한 개인용 검색 엔진입니다.

- 수집은 사용자가 시작한 작업에 한정되며 자동 예약 수집은 없습니다.
- 크롤러는 기본 HTTP fetch와 HTML 추출을 사용하므로 JavaScript로 늦게 렌더링되는 콘텐츠를 완전하게 수집하지 못할 수 있습니다.
- `robots.txt` 처리는 간단한 `User-agent: *` 및 `Disallow` 규칙 중심입니다.
- 검색은 FTS5와 BM25에 기반하며 언어별 형태소 분석, 개인화 랭킹, 스팸 판별은 없습니다.
- 그래프는 읽기 쉬운 소규모 SVG 표현이며 대규모 네트워크 분석 도구는 아닙니다.
- 인증이 필요한 페이지, 캡차, 차단 정책이 있는 사이트는 수집 대상이 아닙니다.
- 데이터 동기화, 계정, 다른 사용자와의 공유 기능은 없습니다.

## 6. 다음 개발 계획

### Phase 1: 개인 검색 품질 강화

- 검색 결과에 본문 미리보기, 헤딩 일치 영역, 색인 시각 표시
- 도메인, 날짜, 노트 여부, 태그를 이용한 검색 필터
- 쿼리 기록과 저장 검색
- 한국어 및 다국어 토큰화/형태소 분석 검토
- 문서 중복 감지와 표준 URL 정규화 강화
- 방문/수집 문서의 재색인 및 오래된 문서 정리 UI

### Phase 2: 크롤링 신뢰성과 제어

- 수집 대기열, 진행률, 중지/재개 버튼
- 사이트별 수집 설정, 제외 경로, 속도 제한
- 더 정확한 robots.txt 파싱과 crawl-delay 처리
- sitemap.xml 발견 및 선택적 수집
- Chromium 렌더링을 활용한 JavaScript 페이지 수집 옵션
- 수집 로그, 실패 원인, 재시도 정책

### Phase 3: 지식 작업공간

- 노트 간 연결과 백링크
- 태그 관리, 노트 검색 고도화, Markdown 내보내기
- 문서별 요약, 핵심 문장 후보, 읽기 목록
- 그래프 확대/축소, 필터, 군집화, 시간 축 보기
- 로컬 데이터 백업 및 가져오기/내보내기

### Phase 4: 제품화

- Electron 패키징 및 자동 업데이트
- 보안 점검: 권한, 외부 탐색, IPC 입력 검증, 콘텐츠 보안 정책
- 오류 보고, 로그, 성능 측정
- 단위/통합/UI 테스트와 CI
- 접근성, 키보드 탐색, 다국어 UI 개선

### Phase 5: 선택적 서버 기반 검색 서비스

이 단계는 개인용 제품 범위를 넘어가므로 별도 설계와 운영 비용 검토가 필요합니다.

- 분산 크롤러, 큐, 스케줄러
- 서버 측 대규모 역색인과 샤딩
- 중복 제거, 스팸 탐지, 랭킹 신호와 링크 분석
- 사용자 계정, 동기화, 권한, 개인정보 및 저작권 정책
- 모니터링, 장애 대응, 비용 관리

## 7. 권장 다음 작업

가장 실용적인 다음 순서는 다음과 같습니다.

1. 수집 진행률과 취소 기능을 추가해 긴 수집 작업을 제어한다.
2. 검색 결과 미리보기와 도메인/날짜 필터를 추가해 재발견 경험을 개선한다.
3. 단위 테스트와 Electron UI 테스트를 추가해 색인, 검색, 세션 복원을 검증한다.
4. 앱 패키징과 데이터 백업 기능을 추가해 일상적으로 사용할 수 있게 만든다.
